[package]
name = "check"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = {version = "3", features = ["derive"]}
html_parser = "0.6.2"
serde = {version = "1", features = ["derive"]}
serde_json = "1"
